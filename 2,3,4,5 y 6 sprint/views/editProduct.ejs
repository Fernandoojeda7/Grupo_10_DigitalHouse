<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head')%>
<body class="body-agregarProd">
    <main class="body-agregarProd">
        <% if (typeof errors != 'undefined') { %>
            <p style="color: rgb(240, 15, 15); font-family: 'Fjalla One', sans-serif; font-size: 1.5vw;">ERROR</p>
            <ul>
                <% for( let i = 0; i < errors.length; i++) { %>
                <li>
                    <%= errors[i].msg%>
                </li>
                <% } %>
            </ul>
        <% } %>
        <h1>FORMULARIO EDICIÓN DE PRODUCTO <%= Producto.name %> </h1>
        <div class="contenedor-agregarProd">
            <form action="/products/edit/<%= Producto.id %>?_method=PUT" method="POST" >
                <div class="form-agregarProd">
                <label>Nombre del Producto</label>
                <input type="text" name="name" value="<%= Producto.name %>">
                </div> 
                <div class="form-agregarProd">
                <div class="form-agregarProd">
                    <label>Precio</label>
                    <input type="number" name="price" value="<%= Producto.price %>">
                </div>
                <div class="form-agregarProd">
                    <label>Descuento</label>
                    <input type="number" name="discount" value="<%= Producto.discount %>">
                </div>
                <div class="form-agregarProd">
                    <label>Tipo:</label>
                    <select name="type" value="<%= Producto.type %>">
                        <option value="promociones">Promociones</option>
                        <option value="novedades">Novedades</option>
                        <option value="ultimas-vistas">Ultimas vistas</option>
                    </select>
                </div>
                <div class="form-agregarProd">
                    <label>Categoría:</label>
                    <select name="category" value="<%= Producto.category %>">
                        <option value="Accesorio">Accesorios</option>
                        <option value="Bicicleta">Bicicletas</option>
                        <option value="Repuesto">Repuestos</option>
                    </select>
                </div>
                <div class="form-agregarProd">
                    <label>Descripción</label>
                    <textarea name="description" cols="30" rows="10" value="<%= Producto.description %>"></textarea>
                </div>
                <button type="submit" class="boton-agregarProd" value="edit">EDITAR</button>   
            </form>
        </div>
    </main>    
</body>
</html>
