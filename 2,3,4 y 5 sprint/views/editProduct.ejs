<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head')%>
<body class="body-agregarProd">
    <main class="body-agregarProd">
        <h1>FORMULARIO EDICIÓN DE PRODUCTO <%= productEdit.name %> </h1>
        <div class="contenedor-agregarProd">
            <form action="/products/edit?_method=PUT" method="POST">
                <div class="form-agregarProd">
                <label>Nombre del Producto</label>
                <input type="text" name="name" value="<%= productEdit.name %>">
                </div>
                <div class="form-agregarProd">
                    <label>Codigo del Producto</label>
                    <input type="number" name="id" value="<%= productEdit.id %>">
                </div>
                <div class="form-agregarProd">
                    <label>Precio</label>
                    <input type="number" name="price" value="<%= productEdit.price %>">
                </div>
                <div class="form-agregarProd">
                    <label>Descuento</label>
                    <input type="number" name="discount" value="<%= productEdit.discount %>">
                </div>
                <div class="form-agregarProd">
                    <label>Tipo:</label>
                    <select name="type" value="<%= productEdit.type %>">
                        <option value="promociones">Promociones</option>
                        <option value="novedades">Novedades</option>
                        <option value="ultimas-vistas">Ultimas vistas</option>
                    </select>
                </div>
                <div class="form-agregarProd">
                    <label>Categoría:</label>
                    <select name="category" value="<%= productEdit.category %>">
                        <option value="Accesorio">Accesorios</option>
                        <option value="Bicicleta">Bicicletas</option>
                        <option value="Repuesto">Repuestos</option>
                    </select>
                </div>
                <div class="form-agregarProd">
                    <label>Descripción</label>
                    <textarea name="description" cols="30" rows="10" value="<%= productEdit.description %>"></textarea>
                </div>
                <div class="form-agregarProd">
                    <label>Seleccionar Imagen</label>
                    <input type="file" name="image" accept=".jpg, .png" class="control-input" value="<%= productEdit.image %>">
                </div>
                <button type="submit" class="boton-agregarProd" value="edit">EDITAR</button>
                <button type="reset" class="boton-agregarProd">BORRAR</button>            
            </form>
        </div>
    </main>    
</body>
</html>
